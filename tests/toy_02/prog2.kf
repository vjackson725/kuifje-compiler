def push(prior, channel):
  elch = set channel;
  elpr = set prior;
  dist = 0;
  for e in elch:
    if (e strIsSub prior):
      eSet = (e filterSet elpr);
      dist <- uniform eSet;
    else:
      skip;
  return dist;

colsPri = set {0, 1};
dataPriD = csv("base.csv", colsPri, 0, "Unique Text");
prior <- dataPriD;

colsCh = set {1};
dataChD = csv("base.csv", colsCh, 0, "Text");
channel <- dataChD;

dist = push(prior, channel);
